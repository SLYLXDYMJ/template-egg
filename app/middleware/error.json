{
  "title": "统一的错误响应",
  "type": "object",
  "properties": {
    "status": {
      "type": "integer",
      "description": "状态码",
      "enum": [
        "400（客户端错误）",
        "401（鉴权失败）",
        "500（服务器错误）"
      ]
    },
    "msg": {
      "type": "string",
      "description": "错误消息"
    }
  },
  "required": [
    "status",
    "msg"
  ]
}